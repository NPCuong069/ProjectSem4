import React from 'react';
import {createStackNavigation} from '@react-navigation/stack';

const RequestScreen =({navigation}) =>(
    <RequestScreen.navigation>

    </RequestScreen.navigation>
)

export default RequestScreen;